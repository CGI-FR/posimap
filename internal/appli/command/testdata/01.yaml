name: single string field - no separator - valid
description: >-
  This is a test for a single string field with no separator.
  The field is valid and should be parsed correctly.
stdin: testdata/01/stdin.fixed-width
flags:
  -s: testdata/01/schema.yaml
loglevel: error
expected:
  stdout: testdata/01/stdout.jsonl
  stderr: testdata/01/stderr.txt
  exit_code: 0
