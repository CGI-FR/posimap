name: nested config files with all capabilities activated
description: >-
  This is a test for nested config files with all capabilities activated.
  The record is valid and should be parsed correctly.
stdin: testdata/07-complete/stdin.fixed-width
flags:
  -s: testdata/07-complete/schema.yaml
loglevel: error
expected:
  stdout: testdata/07-complete/stdout.jsonl
  stderr: testdata/07-complete/stderr.txt
  exit_code: 0
