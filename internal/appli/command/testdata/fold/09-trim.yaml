name: trim option - valid
description: >-
  This is a test for the trim option.
  The record is valid and should be parsed correctly.
stdin: testdata/fold/09-trim/stdin.fixed-width
flags:
  -s: testdata/fold/09-trim/schema.yaml
loglevel: error
expected:
  stdout: testdata/fold/09-trim/stdout.jsonl
  stderr: testdata/fold/09-trim/stderr.txt
  exit_code: 0
