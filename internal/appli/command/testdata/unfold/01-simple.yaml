name: single string field - no separator - valid
description: >-
  This is a test for a single string field with no separator.
  The field is valid and should be parsed correctly.
stdin: testdata/unfold/01-simple/stdin.jsonl
flags:
  -s: testdata/unfold/01-simple/schema.yaml
loglevel: error
expected:
  stdout: testdata/unfold/01-simple/stdout.fixed-width
  stderr: testdata/unfold/01-simple/stderr.txt
  exit_code: 0
