name: multiple field record - no separator - valid
description: >-
  This is a test for a multiple field record without separator.
  The record is valid and should be parsed correctly.
stdin: testdata/unfold/03-multiple/stdin.jsonl
flags:
  -s: testdata/unfold/03-multiple/schema.yaml
loglevel: error
expected:
  stdout: testdata/unfold/03-multiple/stdout.fixed-width
  stderr: testdata/unfold/03-multiple/stderr.txt
  exit_code: 0
