name: nested record with redefines - with separator encoded - valid
description: >-
  This is a test for redefined records with separator encoded.
  The record is valid and should be parsed correctly.
stdin: testdata/unfold/06-redefines/stdin.jsonl
flags:
  -s: testdata/unfold/06-redefines/schema.yaml
loglevel: error
expected:
  stdout: testdata/unfold/06-redefines/stdout.fixed-width
  stderr: testdata/unfold/06-redefines/stderr.txt
  exit_code: 0
